OptimizeTensorExpression rewrites tensor and ML-style expressions into cheaper or fused forms when Sym's tensor rules support it. It does not accept full ProblemScript as input, but it internally builds ProblemScript from the expression and optional shapes. This is a niche but strong capability for tensor graph optimization, not a general-purpose simplifier for ordinary scalar algebra.