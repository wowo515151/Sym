<UserControl x:Class="AGIMyndWPF.Controls.AGIMyndControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button x:Name="StartButton" Click="StartButton_Click">Start</Button>
            <Button x:Name="StopButton" Click="StopButton_Click">Stop</Button>
            <Button x:Name="UpdateGoalButton" Click="UpdateGoalButton_Click">UpdateGoal</Button>
            <Separator />
            <Button x:Name="ClearMemoriesButton" Click="ClearMemoriesButton_Click">Clear Memories</Button>
            <Button x:Name="BrowseMemoriesButton" Click="BrowseMemoriesButton_Click">Browse Memory</Button>
            <Separator />
            <Grid VerticalAlignment="Center" Margin="0,0,8,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" VerticalAlignment="Center" Margin="0,0,6,0">Memory Root:</Label>
                <TextBox Grid.Column="1" x:Name="MemoryRootBox" Width="500" VerticalAlignment="Center" KeyDown="MemoryRootBox_OnKeyDown" />
            </Grid>
            <Grid VerticalAlignment="Center" Margin="0,0,8,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" VerticalAlignment="Center" Margin="0,0,6,0">Max Epochs:</Label>
                <TextBox Grid.Column="1" x:Name="MaxEpochsBox" Width="80" VerticalAlignment="Center" ToolTip="0 = unlimited" Text="500" KeyDown="MaxEpochsBox_OnKeyDown" />
            </Grid>
        </ToolBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Margin="4" Header="Goal">
                <TextBox x:Name="GoalPane" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" />
            </GroupBox>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />

            <GroupBox Grid.Column="2" Margin="4" Header="Log">
                <TextBox x:Name="LogPane" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" IsReadOnly="True" />
            </GroupBox>
        </Grid>
    </DockPanel>
</UserControl>
